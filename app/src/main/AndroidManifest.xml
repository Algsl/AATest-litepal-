<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aatest">

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--<activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>-->
        <activity android:name=".activity.Splash" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".activity.ActivityRecord"/>
        <activity android:name=".activity.ActivityQuery"/>
        <activity android:name=".activity.ActivityStatistics"/>
        <activity android:name=".activity.ActivityAccountBook"/>
        <activity android:name=".activity.ActivityCategory"/>
        <activity android:name=".activity.ActivityUser"/>
        <activity android:name=".activity.ActivityCategoryAddOrEdit"/>
        <activity android:name=".activity.ActivityMain" android:label="AA记账助手"/>
    </application>

</manifest>